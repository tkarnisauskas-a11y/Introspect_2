version: 0.0
Resources:
  - TargetService:
      Type: AWS::EKS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancer:
          ContainerName: "claims-api"
          ContainerPort: 5000
          TargetGroupArn: <TARGET_GROUP_ARN>